## Plan Generated: Unified Thresholds UI Integration

### TL;DR
- Create a single, unified Thresholds UI panel with Chinese labels, a dedicated Save Thresholds action, and startup synchronization with config.json. Ensure the Open Result button remains permanently visible. Consolidate duplicated code paths (main.py and main - 副本.py) into a single source of truth. Deliver a robust test plan including UI, API contracts, and accessibility checks.
- Deliverables: Unified UI panel, central config wiring, startup sync, E2E QA scenarios, and documentation.
- Effort: XL
- Parallel: YES - 3 waves
- Critical Path: Wave 1 → Wave 2 → Wave 3 → Final Verification Wave

## Context
### Original Request
- Integrate and unify Thresholds UI controls, with two-way binding to config.json, and ensure UI reliability and visibility of the Open Result button.
### Interview Summary (from Phase 2)
- Stakeholders require a single source of truth for thresholds (sim_top1_th, diff_top12_th) with safe defaults and clear persistence semantics. Metis identified gaps around threshold types, data sources, rollout governance, and a comprehensive verification plan.
### Metis Review (gaps addressed)
- Documented data model contracts, acceptance criteria, and guardrails for rollout, including feature flags and observability. Outlined edge cases (nulls, TZ, large sets) and migration paths.

## Work Objectives
### Core Objective
- Establish a single Thresholds UI module that drives a consistent, auditable gating experience across the app.
### Deliverables
- Unified threshold UI panel (Sim Top1 Threshold, Top1-Top2 Difference Threshold) with Chinese labels.
- Startup synchronization: load sim_top1_th and diff_top12_th from config.json; apply defaults if use_default_thresholds is true.
- Save Thresholds action: persists current UI values to config.json.
- Ensure Open Result File button remains visible and functional.
- Documentation and a lightweight test plan.
### Definition of Done
- All tasks completed and verified by agent-executed tests; UI reflects config.json; changes persist across restarts; no duplicate UI logic remains.
### Must Have
- Centralized threshold binding (config wiring) in one module; removal of code duplication; robust tests; accessibility checks.
### Must NOT Have (guardrails)
- No regression in unrelated UI panels; no hidden side effects in threshold evaluation.

## Verification Strategy
- ZERO HUMAN INTERVENTION: agent-executed tests for gating logic, UI synchronization, and persistence.
- Test decision: TDD or tests-after; contract tests for API surfaces; UI tests for presence/visibility and layout.
- Evidence: .sisyphus/evidence/task-thresholds-ui.md (to be generated by QA workflow)

## Execution Strategy
### Parallel Execution Waves
- Wave 1: Core consolidation and UI wiring (single source, remove duplicates, binding to config.json)
- Wave 2: Startup sync, Save Thresholds, Open Result button visibility, basic UI tests
- Wave 3: QA Scenarios, accessibility tests, documentation updates, and migration plan

### Dependency Matrix (full, all tasks)
### Agent Dispatch Summary (wave → task count → categories)

## TODOs
- [ ] 1. Consolidate threshold logic into a single module; remove duplication between main.py and main - 副本.py
  - What to do: Merge gating constants, loading/saving, and UI bindings into one module; update imports accordingly.
  - Must Have: Central config wiring, no duplicate state.
  - Parallelization: YES | Wave 1 | Category: quick
  - Acceptance Criteria: Both files reference a single threshold module; sim_top1_th and diff_top12_th come from one source.

- [ ] 2. Implement startup synchronization with config.json
  - What to do: On app startup, read sim_top1_th, diff_top12_th, use_default_thresholds; apply defaults when needed; populate UI.
  - Acceptance: UI shows correct initial values after startup; values match config.json.
  - QA: Verify with use_default_thresholds true/false in config.

- [ ] 3. Implement Save Thresholds action
  - What to do: Persist current UI slider values to config.json when user clicks Save Thresholds.
  - Acceptance: Changes survive restart; no race conditions.

- [ ] 4. Ensure Open Result File button remains visible
  - What to do: Adjust layout so the Open Result File button is never occluded by threshold panel.
  - QA: Manual UI check at various window sizes.

- [ ] 5. Update tests and QA scenarios
  - What to do: Add UI tests for presence, values, and persistence; edge cases: null values, large threshold sets, TZ handling.

- [ ] 6. Documentation and guardrails
  - What to do: Update thresholds-ui-unified.md with final design; update thresholds-verify.md and llm-dispatch-optimization-thresholds.md accordingly.

- [ ] 7. Metis alignment and PRD generation
  - What to do: Prepare PRD with acceptance criteria, rollout plan, and observability guardrails; ensure alignment with governance.

- [ ] 8. Evidence & Verification artifacts
  - What to do: Create .sisyphus/evidence/task-thresholds-ui.md and related artifacts.

## Final Verification Wave (MANDATORY)
- F1 Plan Compliance Audit — oracle
- F2 Code Quality Review — unspecified-high
- F3 Real Manual QA — unspecified-high
- F4 Scope Fidelity Check — deep

## References
- D:\Desktop\新建文件夹 (2)\main.py
- D:\Desktop\新建 文件夹 (2)\main.py
- D:\Desktop\新建 文件夹 (2)\.sisyphus\drafts\thresholds-ui-unified.md
- D:\Desktop\新建 文件夹 (2)\.sisyphus\plans\thresholds-verify.md
- D:\Desktop\新建 文件夹 (2)\.sisyphus\plans\llm-dispatch-optimization-thresholds.md

---
Notes:
- This skeleton is intended to be refined with concrete task descriptions, agent profiles, and acceptance criteria during Wave planning. The next step is to run Metis for governance finalization and then populate the plan with concrete tasks and QA scenarios.
## Self-Review & Gap Classification

- Gap: Threshold types (absolute vs percentile vs dynamic) not fully specified. Decision needed to lock data model and UI representations.
- Gap: Data sources and cadence for threshold evaluation are not finalized; need data contracts and SLOs.
- Gap: Migration path from existing thresholds to unified panel is not defined; decisions needed for rollout strategy.
- Gap: Accessibility and localization scope require confirmation (supported locales, WCAG checks).
- Decision Needed: Which threshold types to support? (Absolute, Percentile, Dynamic) – proposed: Absolute + Percentile; leaving Dynamic for Phase 2 if needed.
- Decision Needed: Startup sync behavior – use_default_thresholds default values vs. config-driven values. Recommended: config-driven with a safe default.
- Decision Needed: Governance approach for rollout (feature flags, staged exposure, rollback).
- Auto-Resolved: None yet. Plan to resolve via Metis review and stakeholder sign-off.
- Defaults Applied: Threshold defaults (sim_top1_th=0.92, diff_top12_th=0.035) assumed to be baseline until config provides overrides.
